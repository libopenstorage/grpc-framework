# Creates docs website

Uses mkdocs to create a website using all the markdown.

For swagger, the `swagger/` directory is a copy of the `dist/` directory from https://github.com/swagger-api/swagger-ui .

